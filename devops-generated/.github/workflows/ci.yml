name: CI - Test and Lint

on:
  pull_request:
    branches: [ main, develop ]
  push:
    branches: [ develop ]

jobs:
  test-analyzers:
    name: Test analyzers
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src/analyzers

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup environment
        run: echo "Setting up TypeScript"

      - name: Install dependencies
        run: echo "No install command"

      - name: Run linter
        run: echo "No lint command"

      - name: Run tests
        run: echo "No test command"

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/coverage-final.json
          flags: analyzers

  test-calculators:
    name: Test calculators
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src/calculators

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup environment
        run: echo "Setting up TypeScript"

      - name: Install dependencies
        run: echo "No install command"

      - name: Run linter
        run: echo "No lint command"

      - name: Run tests
        run: echo "No test command"

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/coverage-final.json
          flags: calculators

  test-generators:
    name: Test generators
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src/generators

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup environment
        run: echo "Setting up TypeScript"

      - name: Install dependencies
        run: echo "No install command"

      - name: Run linter
        run: echo "No lint command"

      - name: Run tests
        run: echo "No test command"

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/coverage-final.json
          flags: generators

  test-tools:
    name: Test tools
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src/tools

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup environment
        run: echo "Setting up TypeScript"

      - name: Install dependencies
        run: echo "No install command"

      - name: Run linter
        run: echo "No lint command"

      - name: Run tests
        run: echo "No test command"

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/coverage-final.json
          flags: tools

  test-types:
    name: Test types
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src/types

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup environment
        run: echo "Setting up TypeScript"

      - name: Install dependencies
        run: echo "No install command"

      - name: Run linter
        run: echo "No lint command"

      - name: Run tests
        run: echo "No test command"

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/coverage-final.json
          flags: types

  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [test-analyzers, test-calculators, test-generators, test-tools, test-types]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Start services with Docker Compose
        run: |
          docker-compose up -d
          sleep 30

      - name: Run integration tests
        run: |
          # Add integration test commands here
          echo "Running integration tests..."

      - name: Stop services
        if: always()
        run: docker-compose down
